###############################################
# Azure AI Foundry Agents Labs Environment File
# 1) Copy to .env  2) Fill values  3) source .env
###############################################

# 필수: 프로젝트 엔드포인트 (Project Overview 에 표기된 프로젝트 URL)
# 예: https://myservice.services.ai.azure.com/api/projects/MyProject
PROJECT_ENDPOINT="https://<ServiceName>.services.ai.azure.com/api/projects/<ProjectName>"

# 필수: 모델 "배포 이름" (기저 모델명이 아닐 수 있음)
MODEL_DEPLOYMENT_NAME="gpt-4o"

###############################################
# Lab 3: MCP Bridge (선택)
###############################################
# MCP 서버 URL (기본값: http://127.0.0.1:8765/mcp)
# MCP_SERVER_URL="http://127.0.0.1:8765/mcp"

# NWS API 사용 시 User-Agent 권장 (연락 가능 정보 포함)
# NWS_USER_AGENT="MyCompanyContact@example.com"

# (고급) 브리지에서 로컬 MCP 프로세스 직접 기동 시 사용 가능
# MCP_SERVER_CMD="python mcp_server.py"
# MCP_SERVER_ARGS=""
# MCP_TOOL_NAME="search"

###############################################
# Lab 4: Remote A2A Orchestrator (선택)
###############################################
# 콤마 구분 원격 에이전트 서버 베이스 URL 목록 (AgentCard 제공 루트)
# REMOTE_AGENT_URLS="http://localhost:8001,http://localhost:8002"

# 개별 지정(둘 중 하나만 써도 됨). 지정 시 REMOTE_AGENT_URLS에 추가로 합쳐짐
# TITLE_AGENT_URL="http://localhost:8001"
# OUTLINE_AGENT_URL="http://localhost:8002"

# 로컬 서버 포트 (server.py 실행 시 참조)
# TITLE_AGENT_PORT=8001
# OUTLINE_AGENT_PORT=8002

###############################################
# 일반 참고
# - 값 수정 후:  source .env
# - 현재 셸에서 확인: echo $PROJECT_ENDPOINT
# - 민감 값은 깃에 커밋하지 마세요.
###############################################
